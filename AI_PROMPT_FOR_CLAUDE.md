# MAZEWARD 対戦AI 学習システム ＋ バランス診断 実装プロンプト

あなたには、Minecraft Java版で動くタワーディフェンス「MAZEWARD」の**対戦モード用AI**と、
**ゲームバランス診断・調整支援の仕組み**、そして**学習ダッシュボードGUI**を実装してほしい。

最初に以下のファイルをすべて読んでから設計・実装を始めること。コードの読解と
ディレクトリ構成の把握には時間をかけてよい。設計判断は必ず「なぜそうするか」を
コメントやメモに残すこと。

---

## 0. 前提となる知識（先に読む）

### ゲームリポジトリ（このリポジトリの中身）
- `c:\Users\tomik\vibe\minecraft\tower_defense`
- ブランチ: `mazeward`（現在の作業ツリーには**未コミットの変更**が多数ある。
  特に `src/main/java/dev/antigravity/mazeward/tower/Look.java` は未コミットの状態で
  **レコードの自動アクセサ `spread()` と同名メソッドが衝突しコンパイルが通らない**。
  AI環境はゲームサーバー（Minestom / Java）をコンパイル・起動する必要がないので
  このエラーは作業をブロックしない。ただし「Java の定義から数値を読む」ツールは
  コンパイラではなく**ソースのテキスト解析**で作ること。）

### 必読ファイル（この順で）
| 目的 | ファイル |
|---|---|
| ゲーム全体像・設計思想 | `README.md` |
| 対戦ルールの実装 | `src/main/java/dev/antigravity/mazeward/versus/VersusMatch.java` `VersusPlayer.java` `Island.java` `AttackerKind.java` `MirrorBot.java` |
| 戦場共通ロジック | `src/main/java/dev/antigravity/mazeward/stage/Battlefield.java` `Shot.java` |
| 塔・敵・地形 | `src/main/java/dev/antigravity/mazeward/tower/TowerKind.java` `enemy/EnemyKind.java` `enemy/Trait.java` `enemy/EnemyInstance.java` `run/Rune.java` `run/Relic.java` |
| グリッド・経路・配置判定（Minestom非依存） | `src/main/java/dev/antigravity/mazeward/core/*`（特に `Grid.java` `PathFinder.java` `PathResult.java` `Shapes.java` `CellType.java`） |
| ウェーブ・生成（シングル用だが参考） | `src/main/java/dev/antigravity/mazeward/stage/Waves.java` `StageGenerator.java` `Stage.java` |
| ヘッドレス検証（既存の考え方） | `src/main/java/dev/antigravity/mazeward/dev/VersusSim.java` `CombatSim.java` `SelfCheck.java` |

### スキル（必ず適用）
- `C:\Users\tomik\.claude\skills\ai-training-dashboard\SKILL.md` と
  `references\ 以下の全ファイル`（trainer-contract / metrics / pitfalls / desktop-app / colab-sync）。
  学習ダッシュボードは必ずこのスキルの取り決めに従うこと。

### GUIの参考実装（外観・起動方式の参考）
- `C:\Users\tomik\vibe\mining_diggers_dream\mining_diggers_dream_2 - モダン\mdd2_colab_rl_ai\`
  - `mdd2_app.py` … Flaskサーバーを裏で起動し、pywebview → Chrome/Edgeアプリモード → 通常ブラウザ の順に窓を開くデスクトップアプリ化
  - `mdd2_web_gui.py` … ダッシュボード本体（Flask + SSE）
  - `MDD2コントロールパネル.vbs` … `pythonw.exe` でコンソールなし起動するランチャー（UTF-16LE保存）
---

## 1. ゲームの対戦ルール（AIが扱う世界）

対戦モード「MAZEWARD VERSUS」の要点。詳細は上記ソースが正。

- **島**: 全員が同一シード生成の同じ盤面（約21x21〜27x27）。グリッドの `OPEN/WALL/ROCK/SPAWN/CORE`。
- **リソース（3つが絡む）**:
  1. **カード（迷路）**: 30秒ごとに1枚だけ手札へ届く（手札上限6）。**コインを使わないが有限**。
     置いた壁は撤去不可。壁の上にしかタワーは置けない（タワーの土台＝壁）。
  2. **コイン**: タワーの設置・強化に使う。10秒ごとにインカム分が入る。**敵を送るとインカムが永久に増える**。
  3. **ストック**: 送り1回につき消費（毎秒1回復、上限30）。送りの種類ごとに消費量が違う。
- **送り**: 生存している全員に同時に1体ずつ湧く。撃破されると送りコストの20%がコインで返り、その10%ぶんインカムが溜まる。
- **勝敗**: ライフ20。モンスターが拠点に到達すると1（終焉騎は上限を奪う）減り、0で脱落。最後の1人が勝ち。
- **タワー**: 最大24基。3段階強化＋最終段階で特化（2択）。売却可（60%返金）。
- **終盤**: 15分を過ぎると漏れダメージ2倍（サドンデス）。

**3要素の絡み合い（学習の対象）**:
- 迷路（カード）は「経路を長く/蛇行させる」と「タワー土台の数を増やす」を同時に満たせない（有限カード）。
- 経路が**蛇行して一つのタワーの射程を何度も通る**ほど、タワーの投資効率が上がる（雷塔の連鎖・砲塔の範囲）。
- タワー（コイン）と送り（コイン）は**同じ財布**。守りに回すか、送ってインカムを伸ばすかが主戦略。
- 飛行敵・瞬移体・庇護者・終焉騎などの能力は「長い迷路＋高DPS塔」の最適解を咎める。

---

## 2. ゴール

1. **対戦（マルチプレイ）で人間と互角以上に戦えるAI** を強化学習で作る。
2. **バランス変更（塔・敵・コストetc の数値修正）に強い**仕組みにする。
   - 数値の変更は「設定ファイル」の変更だけで済み、**再学習なしでもある程度動き、**
     **必要なら再学習できる**。
3. 迷路構築・タワー設置・モンスター送りを**1つの方策（モデル）で結合学習する**。
   **絶対に3つを独立して学習しない**（同じリソースを奪い合うから）。
4. **学習ダッシュボードGUI**（ai-training-dashboard スキル準拠）。
5. **バランス診断機能**: 「強化コストやモンスターのコストがインカムの増え方に対して
   適切か」「モンスターの値段と強さが釣り合っているか」「タワーの値段と火力が釣り合っているか」
   を自動で測り、**数字の推奨値**まで出せる仕組み。
6. **2〜8人のあらゆる対戦人数**（1v1 と FFA 両方）で戦えること。人数によって
   コインの増え方・防御リターン・最適戦略が変わることを学習に含める。
7. **対戦相手の状況を観測**して判断できること。具体的には:
   - 誰かが送ってきた**タイミングに合わせて自分も畳み掛ける**（カウンタープッシュ）
   - 敵の**ライフや迷路・タワーの弱さ**を見て「いま攻めるか、守るか」を決める

---

## 3. 環境（Python の高速シミュレータ）を先に作る

学習・診断は Minecraft サーバーを起動せずに行う。**対戦ルールだけを再現した
Python 環境**を新規に作る。Java実装は「ルールの正」として移植元に使う。

### 3-1. 配置場所と構成（リポジトリ内に新規 `ai/`）
```
tower_defense/ai/
  balance.py               … 全数値の単一の真実（塔・敵・送り・経済・タワー強化式）
  sync_balance.py          … (推奨) Javaソースenumの数値と balance.py を突合/生成
  mazeward_env/
    grid.py pathfinder.py shapes.py
    combat.py reward.py observation.py
    bots_heuristic.py      … 基準ボット（後述）
    rules.py               … ルール定数（30秒カード、サドンデス、ストックetc）
  train.py                 … PPO+リーグ学習（trainer contract準拠）
  balance_analyzer.py      … バランス診断・推奨値出力
  trainer_pb.py            … 共通ロガー（JSON追記・stdout進捗行）
  models/                  … チェックポイント・学習ログJSON（ローカル正本）
tower_defense/dashboard/   … GUI本体（§7）
```

### 3-2. 環境の仕様
- **対戦人数 `num_players` は 2〜8 を環境パラメータとして持つ**。1v1(2人)・FFA(3〜8人)の
  両方を1つの環境クラスで扱い、人数はエピソードごとに変えてよい（§6-2 のドメインランダム化に含める）。
  人数が変わると「送りの受け量・撃破報酬の総量・サドンデスまでの時間的切迫」が変わるので、
  **観測に人数と順位を常に含める**。
- イベントログに他プレイヤーの**送り履歴**（誰が・いつ・何を送ったか）を残し、観測に使えるようにする。
- 盤面は最大27x27。1ステップは「実ゲームの1tick(20tick=1秒)」にはせず、
  **意思決定の粒度でよい**（例: 0.5秒刻み、またはイベント駆動）。ただし**タワーの射撃・弾の
  速度・敵の移動・経路の引直しなどは実ゲームと同じ式**で回す。
- **経路探索は Java の Theta*（任意角度・角抜け禁止・決定論的）と同じ挙動**を移植する。
  早くするため numpy でベクトル化してよい（盤面が小さいので1環境は十分速い）。
- 観測・行動は§4/§5の通り。
- **複数並列環境**（数百〜数千）で動くこと。学習時間の見積もりを出すこと（ローカルCPUで
  何環境/何秒、想定するGPUがあればGPUで何環境か。目安は trainer-contract.md のVRAM表）。

### 3-3. 基準ボット（評価と初期対戦相手）
`VersusSim`/`CombatSim` の貪欲戦略を移植した **3種類の固定ボット**を作る。
- `random_bot`: 完全ランダム（下限確認用）
- `greedy_defense_bot`: 「経路が最も長くなる置き方」＋弓塔を並べ、送りは最小コストのみ
- `income_push_bot`: 序盤から送り最優先でインカムを伸ばす
これらは `win_vs_*` の基準であり、リーグ学習の初期相手でもある。

### 3-4. 学習を速くする実装方針（このゲーム向けの優先順）

**環境は最初から「ベクトル化・スループット重視」で作る。いきなりC++にしないこと。**
優先順は次の通り。

1. **模倣学習（BC）で初期化** — インパクト最大
   - heuristic ボット（§3-3）で対戦ログを大量生成し、行動を教師信号として actor を事前学習。
   - RL はその続きから始める。ランダム初期方策の「迷走タイム」（最初の数万〜数十万ステップ）
     を丸ごと省ける。
   - 注意: BC後は entropy ボーナスと clipping を効かせて「ヒューリスティックの癖」に
     張り付きすぎないようにしてから RL へ移行する。

2. **環境のベクトル化（1プロセスで数千環境）**
   - 環境をループで回さず、numpy 配列のまま全環境を一括計算する。
   - 特に毎ステップ重い部分をベクトル化:
     - タワー射撃判定（タワー数×敵数の距離行列を一括）
     - 射程カバレッジのヒートマップ（各タワー×全セルの距離マスクを一括。観測・報酬両方）
     - 敵の移動・HP減算・スロー/バーン（全環境の配列計算）
   - 観測も 1 つの Tensor（uint8/int16）に詰めて GPU へ流す。

3. **不要なシミュレーションを削る（C++より効く可能性が高い）**
   - **経路再計算は「壁を置いたとき」だけ**（Java実装と同じ設計。毎ステップ全敵の Theta* を
     引き直さない。経路はキャッシュし、配置時のみ更新）。
   - **弾・パーティクル・描画を学習環境から完全に外す**。弾は「発射からの遅延ダメージイベント」
     にモデル化する（射程カバレッジ・命中までの時間は維持）。
   - **イベント駆動 / tickスキップ**: 次の「行動判断」までに進んだ戦闘をまとめて処理する。
   - **意思決定の dt を大きくする**（0.5秒 → 1.0秒刻みなど）。エピソード長が半分になれば
     サンプル数と学習時間も半分になる。§3-2 の 0.5秒刻みは下限の目安で、余裕があれば広げてよい。

4. **カリキュラム学習**
   - まず「2人・小盤面（例15x15）・敵は基本（GRUNT）のみ」で守りと送りの基本を学ばせ、
     盤面サイズ・人数・敵種を徐々に広げる。短いエピソードほど1世代でこなせる試行回数が増える。

5. **高速実装の順序（プロファイル前提）**
   - ① numpy ベクトル化 → ② Numba（JIT）→ ③ 必要なら Cython → ④ pybind11 + C++。
   - 最初に cProfile / torch profiler でボトルネックを測り、**シミュレータが本当に
     ボトルネックだと確認できた場合のみ** ② 以降へ進む。
   - C++ 化するなら「Grid → combat → observation」のコアを 1 モジュールにし、
     Python 環境と同じ step/reset API で**差し替え可能**にする。

6. **学習側の効率**
   - AMP（自動混合精度）、可能なら torch.compile。
   - バッチサイズ最大化（num_envs × rollout steps）。
   - 評価（win_vs_random / win_vs_best）は 5〜10 世代ごとに間引き、評価対局も並列化する。

---

## 4. 観測空間（ルール変更に強いこと）

観測は **生の絶対値ではなく「比・正規化値・導出特徴」** を中心にする。これが
「塔や敵の数値を変えても壊れない」ための最重要ポイント。

### 4-1. 盤面（グリッド画像チャンネル）
- セル種別（壁可/不可・タワー台座可/不可・岩）の one-hot
- 現在の敵経路ポリラインをラスタライズしたもの
- 全タワーの射程カバレッジ・ヒートマップ（これが「蛇行で射程複数回通過」を捉える鍵）
- 敵の現在位置・HP比率

### 4-2. 経済（比率ベース）
- `コイン残高 / 次の送りコスト`、`インカム / 10秒`
- `最安タワーコスト / インカム`、`最安送りコスト / インカム`（「何秒分のインカムで買えるか」）
- `強化コスト(次の段) / インカム`
- `撃破報酬(20%) / 送りコスト` ← 防御側リターン率
- `手札枚数・山札残り・次カード到着秒数`
- サドンデスまでの残り秒・経過秒

### 4-3. ユニット特性（全て正規化）
- 各塔: `DPS/コスト`、`射程/盤面サイズ`、連鎖・範囲・貫通の種別、減速/燃焼/支援の有無
- 各送りモンスター: `HP/コスト`、`速度/コスト`、`リーク効用(HP×速度×リークダメ)/コスト`、`インカム増加/コスト`、`解禁インカム`
- 防御側の推計: `現在の自陣DPS / いま湧いている敵の有効HP合計`（「溶けるか漏れるか」の見積り）

### 4-4. 対戦相手（他プレイヤー）の観測
実ゲームで「見える情報」と「見えない情報」を明確に分けて設計する。

**見える（観測に含めてよい）**
- 他プレイヤーの**残りライフ**（VersusHud のサイドバーで全員分が常時表示される）
- **送り告知**: 誰がいつ何を送ったか（チャットで全員に流れる）。これで
  「いま向こうが攻めてきている」と分かる
- 脱落・勝者
- **島の視察**: プレイヤーは飛んで相手の島へ行けるため、迷路の形・タワーの配置・
  強化状態は実質「見ようと思えば見える」。**学習ではこれを全開示する**
  （＝ゲーム内で実際に取得できる情報だけを、毎ステップ完全に与える）。
  理由:
  1. 実対戦でも取れる情報なので、使い放題にしてよい（実装ギャップがない）。
  2. 部分観測を入れると PPO は「見えない敵状態の推定」を同時に学習しなければならず、
     収束が遅くなる。**全開示で POMDP → MDP に近づけ、単純なフィードフォワード方策で
     そのまま学習できる = 学習は速くなる**。
  3. カウンタープッシュや「弱い相手への追い込み」は、敵の今の姿が分かるほど学習しやすい。
  実装上の注意:
  - 開示するのは「ゲームで本当に見える情報」だけ。敵のコイン残高・インカム・
    ストック・手札は見えないので推定のみ（下記）。
  - 人数 2〜8 で観測次元が変わるため、**固定サイズの符号化**にする。他プレイヤーを
    1 人ずつ特徴ベクトル化（ライフ・直近の送り履歴・タワー数と平均強化段階・迷路の
    経路長推定など）し、**attention / max / mean プーリングで集約**するか、
    最大人数スロット + マスクで固定長にする。
  - 敵の迷路・タワーはピクセルレベルで入れず、要約特徴（経路長・タワー種別別の本数と
    平均Lv・射程カバレッジ合計など）にして観測を小さく保つ。
    「限定的にしてよい」のは学習速度のためではなく、**観測次元の肥大による探索・
    メモリの悪化を避けるため**だけ。情報は落とさず「形を圧縮」して全開示する。

**見えない（推定のみ）**
- 相手のコイン残高・インカム・ストック・手札。送りの頻度や種類から**間接推定**する特徴を作る
  （例: 「相手が最近高コストを送った → インカム/コインが潤沢と推測」）

この観測から以下の判断を学習させる:
- **カウンタープッシュ**: 直近 5〜10秒以内に相手が送ってきた → 防御リソースが割かれている
  タイミングとして、自分も送りを重ねる
- **弱っている相手への追い込み**: 相手のライフが低い・迷路が未完成・タワーが少ない
  → 負けを確定させにいく（サドンデス前の一気押し含む）

---

## 5. 行動空間と結合学習の設計

### 5-1. 方策の形（1モデル・複数ヘッド）
**1つの共有バックボーン**（CNN＋MLP）から出る複数ヘッドで、以下の行動をすべて出す。
別々のネットワークを3つ作らないこと。共有埋め込み＋報酬共有で結合学習する。

- `place_card`: 手札カード(高々6) × 回転(4) を選ぶ → 次に **置き先セル** をマスク付き出力
  （置けるセルだけに softmax。盤面CNNを入力に）→ 「引く/スキップ」も可
- `place_tower`: 塔種別（その時点の解放済み）→ **台座セル**（WALL/ROCKのみ。置けないアクションはマスク）
- `upgrade`/`sell`: 既存タワーを1基選ぶ（頻度が低いので「たまにしか出ないヘッド」でよい）
- `send`: 送りモンスター種別（コスト・ストック・解禁インカムの可否でマスク）／送らない
- `skip`: 何もしない

各ヘッドは同じ方策ネットワークの出力層が分かれているだけ。**価値関数（Critic）は1つ**。

### 5-2. 報酬設計
勝敗が主軸。整形報酬(environment shaping)は主軸を壊さない程度に小さく。

```
R_total =
  +1.0  勝利
  -1.0  敗北
  +毎tickのコイン獲得 × 小さい係数（0.001程度）
  -リーク1回につき (ライフ減少量 × 0.5)
  -終焉騎にライフ上限を奪われたら -0.5
  整形（全体の10%以下に抑える）:
  +0.01 × 正規化した「射程カバレッジ重み付き経路長の増分」
  -0.01 × タワーの遊休率（射程内に敵がいるのに撃っていない割合）
  -0.005 × 手札が上限のまま余している歩数   ← カードを使わない停滞を防ぐ
```

- **「射程カバレッジ重み付き経路長」**: 経路が各タワーの射程円を通過するたび
  1回ぶんの重みを足す。「長いだけ」でなく「一つのタワー射程を複数回通る蛇行」を
  報酬に反映するための指標。学習ログにも `avg_tower_passes` として残す。

### 5-3. 学習アルゴリズムと対戦相手
- 実装しやすく確実な **PPO**（オンポリシー）を推奨。必要なら後で拡張してよい。
- **リーグ自己対戦**: 自分＋過去チェックポイント＋基準ボット（§3-3）のプールから対戦相手を選ぶ。
  metrics.md に従い **`win_vs_random`（下限確認）→ `win_vs_best`（過去最強）→ `elo`** を必ず記録。
- **可能性の分散**: §6-2 のドメインランダム化を混ぜて対戦させる。
- 対局は1v1で行い、時間内に決着しない場合は制限時間で強制終了し「引き分け（0.5/0.5）」扱い。
  `finish_rate` が極端に下がらないよう調整すること。

### 5-4. 対戦人数の扱いとカウンタープッシュ
- **人数をサンプリング**: 学習エピソードごとに `num_players ∈ {2..8}` をランダム/指定で選び、
  同じ方策で全人数に対応させる。評価では **2人・4人・8人を固定で含める**。
- **FFAの報酬**: 勝敗だけでは人数が多いときに報酬が疎になるので、順位に応じた報酬を加える
  （例: 1位 +1.0、2位 +0.2、3位 0、以降 -0.2。1v1はそのまま勝敗）。さらに「脱落するまで
  毎ステップ微小な生存ボーナス」は付けない（生き延びるだけの手抜きを防ぐ）。
- **送りのタイミングエンコード**: 相手の送り履歴を観測（§4-4）に入れ、
  「相手が送った直後 ≒ 防御が手薄」をネットワークが認識できるようにする。
  カウンタープッシュを**強制はせず**、報酬はあくまで勝敗（畳み掛けが有効なら勝率が上がり、
  学習される）。ダッシュボードの指標に `counter_push_rate`（相手送り後10秒以内に自身も送った回数/機会）を
  記録して、この能力が身についたかを外部から確認できるようにする。
- **送り先は全員同時**（ゲームルール）なので「特定の相手を狙う」アクションはない。
  「いつ・何を送るか」と「守りをどこまで固めるか」が判断になる。相手の弱さは
  ライフ順・迷路の完成度・タワー数の推定として観測に載せる。

---

## 6. バランス診断と数値変更への頑健性

### 6-1. `balance.py` = 単一の真実
塔・敵・送り側の全数値は `balance.py` にデータ定義として集める。ゲームのJava enumと
**数値が一致していること**を `sync_balance.py`（テキスト解析）で検証できるようにする。
バランス調整者は **このファイルだけ** を編集する。

### 6-2. ドメインランダム化（数値変更に強い学習）
訓練エピソードごとに `balance.py` の値に ±20% 程度のスケールを掛ける（コスト・HP・
ダメージ・クールダウン・インカム増加・カード間隔など。**ランダム化してよい項目の
ホワイトリスト**を設ける）。これにより「特定の数値を覚える」のではなく
「比と構造で判断する」方策になる。GUIからランダム化の強さを変えられること。

### 6-3. `balance_analyzer.py`（診断・推奨値）
自動で以下を計算し、人間が読めるレポート（表＋警告＋**数値の推奨値**）を出す。

**経済の健全性**
- `収入曲線`: 送り再投資を仮定した `インカム(t)`・`累計コイン(t)`（§1のルールから算出）
- `強化コスト / インカム`: 「次の強化1段に必要なインカム待ち秒数」。全塔の中央値が
  例えば60秒を超えていたら高すぎ、などの閾値とともに提示
- `モンスター価格 / 撃破報酬(20%)`: 送った相手に倒されたときのコイン回収率

**ユニットの釣り合い**
- 塔: `実効DPS / 全コスト(コイン＋土台カード分)` を塔ごとに算出（単体・範囲・連鎖・
  貫通それぞれ、代表敵への実効装甲込み）。**弓塔を基準(1.0)にした比**を出し、
  「1.3倍超＝強すぎ」「0.6倍未満＝弱すぎ」をフラグ
- モンスター: `効用(HP×速度×リーク) / コスト`・`リークダメ / コスト`・`インカム増 / コスト`。
  どの解禁帯でも「常に最適な1種類」に収束していないかを検出（多様性チェック）
- 迷路: `平均経路長 / 盤面サイズ`、`同一タワー射程の平均通過回数`、`到達までの平均秒数`

**対称性チェック（対戦バランス）**
- 「ガチガチの守り」ボット vs 「送り特化」ボットの勝率が 40〜60% の範囲に収まっているか。
  片方に偏る場合はルール側（コスト・インカム・リークダメ）のどれを触るべきかまで出力。

**人数別のバランス（2〜8人で変わる部分）**
- `収入曲線(人数)`: 送りが全員に同時に届くため、**人数が増えるほど防御側の撃破報酬総量が
  増える**。`n人戦での1回の送りに対する全世界の合計報酬` を n ごとに計算し、
  「送りスパムが過剰に美味しい人数」「守り一辺倒が強すぎる人数」を検出する。
- 送りコスト・インカム増加・リークダメの**推奨値を人数別**に出せるようにする
  （例: 8人戦では送り1回の世界全体リターンが3倍以上なら、撃破報酬を下げる提案など）。
- サドンデスまでの「標準的な決着秒数」を人数別にシミュレートし、FFAが長時間化しないか確認。

**推奨値出力**: フラグが出た項目には「(例) 砲塔 ダメージ 26 → 30 推奨」のように
具体的な変更候補を出す。これは人間が確定する（自動適用はGUIのボタン操作にする）。

### 6-4. 数値変更後の扱い
- バランスを変えたら `balance_analyzer.py` が「**再学習が必要か**」の参考値も出す
  （純粋な数値スケールの変更なら学習継続、行動空間やルールの形が変われば再学習）。
- 訓練途中に `balance.py` を変えた場合、変化を検知して学習ログに記録すること。

---

## 7. ダッシュボードGUI（ai-training-dashboard スキル準拠）

必ず `SKILL.md` と `references/` を読んでから作る。取り決めは厳守:
- 世代ごとの**JSON追記ログ**（`episode`, `timestamp` 必須、勝率は0〜1、最大2000件）
- **stdout進捗行は必ず改行**（`\r` 禁止。isattyで出し分け）
- 例外は握りつぶさず終了コードで失敗を伝える
- 設定は環境変数で渡す（`<PREFIX>_MAX_GENS`, `<PREFIX>_NUM_ENVS` 等）
- pitfalls.md の一覧を最後に全部チェック（特に `\r`、文字コード、ポート判定、pythonwのstdout）

### タブ構成（テンプレート＋追加）
| タブ | 内容 |
|---|---|
| 進捗 | 現在値タイル＋世代一覧表（**ツールチップだけにしない**） |
| グラフ | 指標推移。**2軸グラフ禁止・small multiples**。dataviz スキルを読むこと |
| 対局ビューア | 盤面文字列＋タワー配置＋敵経路＋送り履歴のリプレイ（テキスト表示でよい） |
| バランス | §6-3 のレポート表示＋推奨値＋「balance.py を適用」ボタン |

### 記録する指標
- 汎用: `loss` `kl` `entropy` `fps` `seconds_per_gen` `games_finished` `num_envs` `finish_rate` `avg_turn` `win_vs_random` `win_vs_best` `elo`
- 固有（MAZEWARD）: `avg_path_length` `avg_tower_passes` `card_usage_rate` `sends_per_game` `leaks_per_game` `avg_income_final` `avg_coin_efficiency` `tower_count_final`
- 人数/相手: `num_players`(全体) `counter_push_rate` `pressured_finish_rate`(相手が低ライフ時に送った試合の決着率) `win_rate_by_players`(人数別勝率。入れ子dictで記録)

### 起動方式（MDD2 コントロールパネルと同様）
- `dashboard/app.py` … Flaskを裏起動 → pywebview → Chrome/Edgeアプリモード(`--user-data-dir`)
  → 通常ブラウザ の順に窓を開く
- `コントロールパネル.vbs` … `pythonw.exe` でコンソールなし起動（**UTF-16LE(BOM付き)で保存**、
  pythonw時のstdout回避・ログファイル化）
- `start.bat` … コンソール付き起動（エラーが見える）
- 空きポート判定に `SO_REUSEADDR` を付けない
- (任意) Colab連携は `references/colab-sync.md` に従う（Drive永続化・ローカル/Colab切替）

---

## 8. 受け入れ条件（最後に必ず確認）

1. `ai/` に Python 環境が一通り動く（依存は `requirements.txt` or `pyproject.toml`）。
2. **1世代でもいいので実際に学習を回し**、以下の通し確認をする:
   - GUIと同じパイプ経由で進捗行が届く（`\r` で固まらない）
   - JSONログが episode 付きで追記される
   - `win_vs_random` が出る（ランダムボットに勝てる＝最低限のゲーム理解）
   - `finish_rate` が高く（例: 90%超）、引き分けが増えていない
   - グラフ・表がダッシュボードに表示される
3. **結合学習の確認**: 塔だけ・送りだけ等の単独学習モードが**存在しない**ことを説明できる。
4. `balance_analyzer.py` が現行 `balance.py` に対してレポートを出し、最低2項目は
   具体的な推奨値を含んでいる。`sync_balance.py` がJava enumとの不一致を検出/一致を確認する。
5. `balance.py` の数値を1つ変える → 診断結果と（可能なら）学習挙動が変わることを確認。
6. **人数対応の確認**: 2人・4人・8人の3条件で学習・評価が動き、`win_rate_by_players` が
   記録されている（入れ子dict）。
7. **相手観測の確認**: 相手のライフ・送り履歴（カウンタープッシュに使う情報）が観測に
   含まれていることをコードで示せる。評価時は `counter_push_rate` が記録される。
8. 各設計判断に「なぜ」が残っている（コードコメント or 実装メモ）。

---

## 9. スコープ外（やらないこと）
- Minecraftサーバー（Java）本体の改修・コンパイルエラー修正（`Look.java`含む）
- クライアントMOD / リソースパック
- シングル（ウェーブ制）の学習（対戦モードを優先。ただし環境は共存できる形に）
- 人間と本番サーバーでリアルタイム対戦するためのJava推論組み込み（**将来的に
  ポリシーをJSONエクスポートして使える形に留める**。どこに何を出すかだけ設計メモを残す）

---

## 10. 質問すべき点（曖昧なまま進めない）
- 学習リソース: 自前GPUかColabか、VRAMはどのくらいか
- 学習時間の予算（例: 一晩でこれだけ進める、など）
- GUIの日本語/英語表示どちらを主にするか
（対戦人数 2〜8・FFA 対応は上記仕様で確定。学習時の人数分布の偏りだけ必要なら指定）