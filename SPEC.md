# MinestomローグライクTowerDefense 仕様整理

最終更新: 2026-04-02

この文書は、実装済みコードを基準に整理した仕様です。過去メモに含まれていた構想要素と、現在実装との差分も明記します。

## 1. 実装済みサマリ

### 1.1 ゲーム全体
- Minestomサーバー上で動作
- ロビー -> ロードマップ -> ステージの遷移
- ロードマップは層進行（次層解放式）
- ステージで準備時間30秒後にウェーブ進行

### 1.2 コマンド
- `!lobby`: ロビーに戻る
- `!roadmap`: ロードマップを表示
- `!start`: ステージ内でウェーブ開始
- `!state`: Wave / HP / Gold / Enemies / Towers 表示
- `!deck <1-3>`: デッキを確定
- `!tower <type>`: タワー設置（`basic|flame|frost|ball|poison|snowball`）

### 1.3 敵システム（新実装）
- 敵タイプ: 通常敵、早い敵、装甲敵、ボス敵
- 敵パラメータはWaveと層に応じてスケーリング
  - HP: Wave毎 +16%、層毎 +22%
  - 速度: Wave毎 +7%、層毎 +9%
  - 報酬ゴールド: 敵タイプ+Wave+層に応じて計算
- Wave進行に応じた敵構成の動的変化
  - Wave 1-2: 通常敵90%以上
  - Wave 3-4: 装甲敵10%混入開始
  - Wave 5-6: 装甲敵20%まで増加
  - Wave 7-8: ボス敵5%登場
  - Wave 9+: 各敵タイプがバランスよく出現
- 敵タイプごとの特性:
  - 通常敵(NORMAL): バランス型、コスト低い
  - 早い敵(FAST): 高速・脆い、報酬中程度
  - 装甲敵(ARMORED): 高HP・遅い、報酬高い
  - ボス敵(BOSS): 超耐久・高報酬

### 1.4 デッキとビルド操作
- ロードマップ入場時にデッキ選択GUIを表示
- 3候補から1つ選択（詳細GUIあり）
- ステージ入場時はデッキ操作用アイテムを配布
- ホットバー挙動:
  - スロット1: タワー選択カテゴリを開く
  - スロット2: ブロック選択カテゴリを開く
  - カテゴリ画面の戻るアイテムでカテゴリ選択に戻る
- 建築操作:
  - 1回目右クリックで対象を選択
  - 同じ対象を再度右クリックでプレビュー先へ設置
- ブロックは在庫消費あり、タワーは在庫非消費（ただしゴールド消費あり）

### 1.5 戦闘と経済
- 初期ゴールド: 50
- 拠点HP: 20
- 敵撃破ゴールド: 敵タイプごと（3～20+補正）
- Waveクリア報酬: `10 + Wave * 2`
- 敵到達で拠点HP減少

### 1.6 タワー
- 6種実装済み
  - basic, flame, frost, ball, poison, snowball
- サイズ対応:
  - 1x1, 1x2, 1x3, 2x2
- 非1x1は複数エンティティで見た目表現
- 状態異常系ラベルはあるが、現状の攻撃は単体ダメージ中心

## 2. 実装と记述の比較

| 項目 | 以前の記述 | 現在実装 | 判定 |
| --- | --- | --- | --- |
| コマンド | `!start`, `!tower`, `!state`中心 | `!lobby`, `!roadmap`, `!deck` も実装済み | 記述更新済み |
| Wave開始文言 | `!wave 1` 開始と記述 | 実装コマンドは `!start` | 一致 |
| デッキ操作 | 選択GUI中心の説明 | GUIに加え、ステージ中のカテゴリ切替（1=タワー,2=ブロック,戻る）実装 | 記述更新済み |
| タワー配置条件 | ブロック上限定など記述あり | 実装は経路近接/重複禁止。高さはプレビュー座標ベース | 一致 |
| 報酬カードUI | クリア後カード選択と記述 | 未実装 | 予定 |
| ボスバー | 実装予定扱い | 未実装 | 予定 |
| 敵タイプ多様化 | 装甲・高速・ボス特殊など | **実装完了** | 完了 |
| 敵HP/報酬スケーリング | 不明 | **Wave/層別スケーリング実装** | 実装完了 |

## 3. 仕様（実装基準）

### 3.1 ワールド
- Lobby: 初期スポーンとロードマップ導線
- Roadmap: 層付きノード選択、進行ロックあり
- Stage: 敵経路、準備時間、配置と戦闘

### 3.2 ステージ進行
1. ステージ入場
2. 準備時間30秒
3. `!start` でWave開始（または準備終了後の導線）
4. 敵全滅でWaveクリア
5. ロードマップ側の解放層を更新

### 3.3 配置ルール
- ブロック:
  - 形状ごとのオフセット配置
  - 保護セル/範囲外/重複は拒否
  - 成功時に在庫1消費
- タワー:
  - ゴールド不足時は拒否
  - 既存タワー占有重複は拒否
  - 経路近傍は禁止

### 3.4 UI
- デッキ選択GUI（ロードマップ）
- デッキ詳細GUI（戻る/確定）
- ステージ中ホットバーカテゴリ切替
- ActionBarで準備時間表示

## 4. 未実装・今後予定

- Waveクリア後の報酬カードUI
- 敵タイプごとのタワーペナルティ/ボーナス効果（現在はすべてのタワーが等しくダメージ）
- 状態異常（DoT, スロー, 連鎖など）のゲーム効果実装
- メタ進行
- ボスバー等のUI強化

## 5. 更新方針

- このファイルは「実装準拠」を優先する
- 構想を書く場合は、必ず「未実装」セクションに分離する
- 実装変更時は「2. 実装と記述の比較」を先に更新する
