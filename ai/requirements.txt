# MAZEWARD VERSUS 学習環境
#
# torch は環境ごとに正しいビルドが違う（CUDA / CPU）。ここではバージョンだけ
# 指定して、CUDA 版が要る場合は PyTorch の公式インデックスから入れること:
#   pip install torch --index-url https://download.pytorch.org/whl/cu118
numpy>=1.26
torch>=2.1
flask>=3.0

# 任意（強く推奨）: 経路探索の JIT。無くても結果は同じで、遅くなるだけ。
# Theta* が実測 30 倍・到達判定が 49 倍になり、1 世代の実時間が約 2 割縮む。
# Colab には最初から入っている
numba>=0.59

# 任意: ダッシュボードをネイティブウィンドウで開く（無ければ Chrome/Edge の
# アプリモードに自動で落ちるので必須ではない）
# pywebview>=5.0

# 任意: Colab の学習ログをローカルへ取り込む場合のみ
# google-api-python-client>=2.100
# google-auth-oauthlib>=1.2
